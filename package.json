{
  "name": "product-catalog-scraper",
  "version": "1.0.0",
  "description": "A two-stage Node.js pipeline that extracts product URLs from saved category HTML and converts product pages into structured JSON and CSV records.",
  "main": "src/cli.js",
  "scripts": {
    "start": "node src/cli.js",
    "extract:urls": "node src/cli.js urls",
    "extract:products": "node src/cli.js products",
    "test": "node --test"
  },
  "keywords": ["nodejs", "web-scraping", "cheerio", "axios", "etl", "data-pipeline"],
  "license": "MIT",
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "^1.0.0"
  }
}
